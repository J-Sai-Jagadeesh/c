#include <stdio.h>
int main() {
int a,b,c;
printf("enter a,b,c values:");
scanf("%d %d %d",&a,&b,&c);
if(a>b)
{
 if(a>c)
{
    printf("a is largest 5d",a);
}
}
else if(b>c)
{
    printf("b is largest %d",b);
}
else
{
    printf("c is largest %d",c);
}
}
